<%= form_for(current_user.watchlist.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :watchlist_id, @movie.id %></div>
  <%= f.submit "Add to watchlist", class: 'btn btn-primary watch-button' %>
<% end %>